# NOC Pre-Triage Agent — Python Dependencies
# Python 3.11+ required
#
# Install all:        pip install -r requirements.txt
# Install core only:  pip install pydantic pyyaml requests
# Install RAG:        pip install chromadb sentence-transformers
# Install live mode:  pip install netmiko napalm

# ── Core (always required) ────────────────────────────────────────────────────
pydantic>=2.0,<3.0
pyyaml>=6.0
requests>=2.31.0

# ── RAG — Local vector search (required for runbook retrieval) ────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ── LLM — Ollama is a local server; this is its Python client ────────────────
# Ollama itself must be installed separately: https://ollama.com/download
# The app communicates with Ollama via HTTP — no additional Python package needed.
# Kept here as a comment for documentation.
# ollama>=0.1.0   # Optional: use the official ollama Python SDK instead of raw requests

# ── Live device mode (optional — not needed for --mock) ───────────────────────
netmiko>=4.3.0
napalm>=5.0.0

# ── Secrets (optional — for HashiCorp Vault integration in Phase 2) ───────────
hvac>=2.0.0        # HashiCorp Vault client

# ── Dev/Test ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
